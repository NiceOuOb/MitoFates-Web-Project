{
  "name": "mitofates-web",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "dev-start": "nodemon server.js",
    "init": "sudo apt update && sudo apt install -y nodejs npm && sudo apt install -y libsvm-tools libmath-cephes-perl libperl6-slurp-perl libinline-perl && sudo ln -sf /usr/bin/svm-predict /usr/bin/svm_predict && sudo ln -sf /usr/bin/svm-scale /usr/bin/svm_scale",
    "install": "echo \"Installing MitoFates:\" && git clone https://github.com/ken1roimai/MitoFates.git ../MitoFates",
    "setup": "npm ci --omit=dev",
    "start": "node server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "multer": "^2.1.1"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
